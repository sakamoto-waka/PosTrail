%nav.navbar.navbar-expand-lg.navbar-light.py-3.px-md-5.px-sm-3
  // urlの中にadminが含まれていたら
  -if request.path.match(/\/admin(\/)?/) || admin_signed_in?
    = render 'layouts/admin_header'
  -else
    = render 'layouts/user_header'