= (user_signed_in? && current_user == @user) ? (content_tag :h1, 'マイページ') : (content_tag :h1, "#{@user.name}さんのプロフィール")
// - if current_user.name == "ゲストユーザー"
//   .text-center 
//     ゲストユーザーなのでプロフィールはありません
// - else
= render 'user_show', user: @user, tags_list: @tags_list
  
